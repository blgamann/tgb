import React from "react";

const Logo: React.FC = () => (
  <section
    id="logo"
    className="h-screen flex justify-center items-center text-4xl bg-gray-100"
  >
    <h2>Logo Section</h2>
  </section>
);

export default Logo;
